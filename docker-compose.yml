version: '3.8'

services:
  ufc-scraper:
    build: .
    container_name: ufc-scraper
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    environment:
      - TZ=UTC
    restart: unless-stopped
