version: '3.8'

services:
  ufc-scraper:
    build: .
    image: ufc-scraper:latest
    container_name: ufc-scraper-app
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    environment:
      - TZ=UTC
    restart: unless-stopped
