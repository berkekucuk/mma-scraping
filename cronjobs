*/3 * * * 6,0 cd /app && /usr/local/bin/python run_live_scraper.py >> /app/logs/live_events.log 2>&1

15 * * * * cd /app && /usr/local/bin/scrapy crawl smart >> /app/logs/uncompleted_events.log 2>&1

# Bu satırı silmeyin.
